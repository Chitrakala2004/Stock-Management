{
  "name": "stock-management",
  "version": "1.0.0",
  "description": "Stock Management Application - React Frontend & Node.js Backend",
  "private": true,
  "scripts": {
    "client": "cd src && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd src && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
